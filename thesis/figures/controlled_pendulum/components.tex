\providecommand{\potR}{0.85}
\providecommand{\trackR}{0.63}
\providecommand{\ang}{0}

\tikzset{
  coordsys/.pic={
    \fill[red] (0,0) circle(0.05);
    \draw[->,thick, red] (0,0) -- (1.4,0) node[right] {$x$};
    \draw[->,thick, red] (0,0) -- (0,1.4) node[above] {$y$};
  }
}

\tikzset{
  potentiometer/.pic={
  \def\R{3}      % outer radius
  \def\r{2.1}    % resistive track radius

  \draw[thick] (0,0) circle (\R);

  % --- resistive track (arc) ---
  \draw[line width=12pt] (-225:\r) arc (-225:45:\r);

  % --- center shaft ---
  \draw[line width=5pt, gray!70] (0,0) circle (0.6);

  % --- wiper arm ---
  \draw[line width=5pt, gray!70] (-45:0.6) -- (-45:\r);

  % --- terminals ---
  \draw[line width=5pt, gray!70]
      (90:0.6) -- (90:4);

  \draw[line width=5pt, gray!70]
      (-225:\r) -- ++(-225:2);

  \draw[line width=5pt, gray!70]
      (45:\r) -- ++(45:2);


  % --- terminal labels ---
  \node at (-2.8,3.4) {\Large A};
  \node at (2.8,3.4) {\Large B};
  \node at (0,4.5) {\Large W};

  % --- rotation arrows ---
  \draw[<->, thick]
      (0:1.2) arc (0:-90:1.2);
  }
}
