\section*{Notation and Conventions}
\addcontentsline{toc}{section}{Notation and Conventions}
\label{sec:20_notation_and_conventions}

The following symbols (see Table~\ref{tab:notation}) are used consistently throughout Chapter~\ref{chap:theoretical_background}.
Time-dependent arguments are written explicitly on first introduction and when a named time instant is referenced.
When the dependence on time is clear from context, the argument $(t)$ may be omitted.

\begin{table}[htbp]
    \centering
    \small
    \begin{tabular}{ll}
        \toprule
        Symbol & Meaning \\
        \midrule
        $t$ & Physical time \\
        $t_e$ & Event time \\
        $i$ & Subsystem index \\
        $j$ & Event or indicator index \\
        $k$ & Communication-point index \\
        $\nu$ & Superdense-time micro-step index \\
        $x(t)$ & Continuous or differential state vector \\
        $z(t)$ & Algebraic variable vector \\
        $q(t)$ & Discrete mode or discrete state \\
        $u(t)$ & Input vector \\
        $y(t)$ & Output vector \\
        $f(\cdot)$ & State-transition map \\
        $h(\cdot)$ & Output map \\
        $g(\cdot)$ & Algebraic constraint function \\
        $G(\cdot)$ & Implicit DAE residual form \\
        $\gamma_j(\cdot)$ & Event indicator (zero-crossing function) \\
        $R_j(\cdot)$ & Continuous-state reset map \\
        $\delta_j(\cdot)$ & Discrete-state update map \\
        $(\cdot)^-$ & Value immediately before an event \\
        $(\cdot)^+$ & Value immediately after an event \\
        $(t,\nu)$ & Superdense time \\
        $T_k$ & Communication point $k$ \\
        $H_k = T_{k+1} - T_k$ & Macro step size \\
        $H$ & Generic requested step duration \\
        $\Delta t_{i,r}$ & Internal step size of subsystem $i$ at local step $r$ \\
        $\mathsf{SU}_i$ & Abstract simulation unit representation \\
        $I$ & Finite index set of simulation units \\
        $\mathcal{S}_i$ & Internal state space of simulation unit $i$ \\
        $\mathcal{U}_i$ & Set of input ports of simulation unit $i$ \\
        $\mathcal{Y}_i$ & Set of output ports of simulation unit $i$ \\
        $\mathcal{V}_i$ & Value domain of the ports of simulation unit $i$ \\
        $j \rightarrow i$ & Directed dependency edge from producer simulation unit $j$ to consumer simulation unit $i$ \\
        $\mathcal{U}$ & Global set of indexed input ports \\
        $\mathcal{Y}$ & Global set of indexed output ports \\
        $F_i$ & Direct-feedthrough relation of simulation unit $i$ \\
        $\mathcal{L}$ & Set of directed port connections in a co-simulation scenario \\
        $\mathcal{C}$ & Co-simulation scenario \\
        $A \subseteq I$ & Set of simulation units in one algebraic loop \\
        $\Gamma_i$ & Coupling operator mapping outputs to the inputs of simulation unit $i$ \\
        $\Gamma_A$ & Coupling operator restricted to one algebraic loop \\
        $\hat{u}_i(t)$ & Extrapolated input of simulation unit $i$ between communication points \\
        $s_i(T_k)$ & Internal state of simulation unit $i$ at communication point $T_k$ \\
        $\operatorname{set}_i$ & Input staging operator of simulation unit $i$ \\
        $\operatorname{get}_i$ & Output query operator of simulation unit $i$ \\
        $\operatorname{step}_i$ & Time-advancement operator of simulation unit $i$ \\
        $\mathcal{R}(\cdot)$ & Interface residual for algebraic-loop consistency \\
        $U_A$ & Stacked interface inputs of one algebraic loop \\
        $Y_A(U_A)$ & Interface outputs induced by a trial input vector $U_A$ \\
        $G=(V,E)$ & Directed dependency graph for structural analysis \\
        \bottomrule
    \end{tabular}
    \caption[Notation]{Notation used throughout Chapter~\ref{chap:theoretical_background}.}
    \label{tab:notation}
\end{table}

In the physical and mathematical discussion, the term \emph{subsystem} denotes a constituent part of the modeled system.
In a tool-neutral co-simulation context, the executable software unit that encapsulates a subsystem model is called a \emph{simulation unit}.
In \emph{SysSimX}, the concrete realization of this concept is called a \emph{component} and is implemented through the \texttt{CoSimComponent} interface.

In implementation and case-study contexts (Chapters 5–6), the macro step size is abbreviated as $\Delta t$ when the communication-point index is clear from context.
