%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Multi-Model Component and Mode Switching}
\label{sec:impl_multi_model}

This section describes the implementation of runtime model switching inside one component.
It addresses UR-09 and the derived requirements SR-09-01 to SR-09-04 in Table~\ref{tab:req_heterogeneous_integration}.
Runtime model switching is required when one physical subsystem is best represented by different models in different operating regimes, such as switching from a high-fidelity contact model to a reduced-order rigid-body model when contact ends.
The architectural role of the \texttt{MultiComponent} abstraction was introduced in Section~\ref{sec:architecture_multicomponent}.
The present section focuses on the concrete implementation in \texttt{multi\_comp.py}.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\subsection{Scope and Component Contract}
\label{sec:impl_multi_model_scope}

\texttt{MultiComponent} extends \texttt{CoSimComponent} with a registry of interchangeable internal models.
Each internal model is a \texttt{CoSimComponent}.
The surrounding \texttt{System} interacts only with the wrapper component.
It does not need to know which internal model is active.

At runtime, exactly one model is active.
The wrapper identifies it through \texttt{active\_mode} and accesses it through \texttt{active\_comp}.
The active model performs the time step, provides the wrapper outputs, and serves as the source for state access and hybrid hooks.
Input assignment is handled separately, because incoming inputs are forwarded to all registered models.
A user-defined \texttt{mode\_selector} may request a different mode before a step is executed.
On an accepted switch, the wrapper transfers the physical state from the current model to the target model.

Subclasses provide the model-specific parts of this process.
They register the available models and define how state is adapted between model interfaces.
Table~\ref{tab:multi_component_contract} summarizes the main contract.

\begin{table}[tbp]
  \centering
  \caption{Main extension points and state fields of \texttt{MultiComponent}.}
  \label{tab:multi_component_contract}
  \begin{tabular}{lp{9.5cm}}
    \toprule
    \textbf{Element} & \textbf{Role} \\
    \midrule
    \texttt{models} & Stores the available model implementations by mode key \\
    \addlinespace
    \texttt{active\_mode} & Stores the key of the currently active model \\
    \addlinespace
    \texttt{active\_comp} & References the currently active component instance \\
    \addlinespace
    \texttt{mode\_selector} & Selects the desired mode from time and current state \\
    \addlinespace
    \texttt{hysteresis} & Blocks rapid switching when a dwell time is configured \\
    \addlinespace
    \texttt{\_adapt\_state()} & Maps the exported state to the target model interface \\
    \addlinespace
    \texttt{sync\_events} & Records performed switches for inspection and debugging \\
    \addlinespace
    \bottomrule
  \end{tabular}
\end{table}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\subsection{Initialization and Interface Unification}
\label{sec:impl_multi_model_initialization}

A concrete subclass registers its internal models during construction.
It stores them in \texttt{models}, selects the initial model through \texttt{active\_mode}, and stores the corresponding component in \texttt{active\_comp}.
The subclass then calls \texttt{\_unify\_ports()} before the wrapper is registered with a \texttt{System}.
This construction-time setup is required because signal connections are validated before component initialization.

The wrapper must expose one stable port interface to the surrounding system.
For this purpose, \texttt{\_unify\_ports()} adopts the input and output specifications of the active component.
It then checks that every registered model provides compatible ports with the same names, directions, port types, and units.
If a model misses a required port or exposes an incompatible specification, construction fails with a \texttt{ValueError}.
This ensures that a later mode switch cannot change the external interface of the component.

The same consistency requirement applies to direct feedthrough metadata.
\pyfn{\_detect\_direct\_feedthrough()} compares the \texttt{direct\_feedthrough} dictionaries of all registered models.
A mismatch is rejected at initialization.
The structural analysis of Section~\ref{sec:impl_structural} assumes that each component keeps one fixed zero-delay behavior during a simulation run.
A switching component must therefore not change its feedthrough property at runtime.
The \texttt{MultiComponent} can therefore switch the internal model without changing the dependency graph seen by the \texttt{System}.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\subsection{Runtime Mode Switching}
\label{sec:impl_multi_model_switching}

Runtime switching is part of the component step.
When a master algorithm calls \texttt{do\_step(t, dt)} on the wrapper, the base component logic delegates to \texttt{\_do\_step\_internal(t, dt)}.
The method first reads the state of the active model and evaluates the optional \texttt{mode\_selector}.
If no selector is configured, the active mode remains unchanged.
Figure~\ref{fig:impl_multi_model_switching} summarizes the switching path.

\begin{figure}[t]
    \centering
    \includegraphics[width=0.82\textwidth]{diagrams/out/pdf/execution/MultiComponent_Switching.pdf}
    \caption{Implementation-level mode switching sequence of \texttt{MultiComponent}.
    The master algorithm calls the wrapper like any other component.
    The wrapper evaluates the selector and optional hysteresis.
    If a switch is accepted, the state is adapted and written to the target model before the active step continues.}
    \label{fig:impl_multi_model_switching}
\end{figure}

A configured \texttt{Hysteresis} object may block a requested switch.
It checks whether the proposed mode differs from the last accepted mode and whether the dwell time has elapsed.
If the check fails, the wrapper keeps the current mode for this step.
This prevents rapid switching near decision boundaries.

If the requested mode is accepted, \texttt{\_switch\_mode()} performs the transition.
It first retrieves the current state from the active model.
The state is then passed to \texttt{\_adapt\_state()}.
This hook maps the state to the format expected by the target model.
The adapted state is written to the target component through \texttt{set\_state()}.
The wrapper then updates \texttt{active\_mode} and \texttt{active\_comp}.

Each accepted switch is stored in \texttt{sync\_events}.
The stored entry contains the switch time and the two involved modes.
It also stores the retrieved state and the synchronized target state.
This log is used to inspect whether the state transfer behaved as expected.

After the optional mode switch, the actual time step is delegated to the active component through \texttt{do\_step(t, dt)}.
The master algorithm therefore does not need a special execution path for multi-model components.
The internal mode selection remains encapsulated inside the wrapper.
Section~\ref{sec:impl_multi_model_state_events} describes how input forwarding, output propagation, and hybrid delegation are handled around the active model.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\subsection{Input, Output, and Hybrid Delegation}
\label{sec:impl_multi_model_state_events}

The wrapper must keep inactive models ready for future switches.
For this reason, \texttt{set\_inputs()} forwards incoming input values to all registered models.
Only the active model advances in time.
All models nevertheless receive the latest external inputs.
This avoids stale input values when a later step switches to another model.
Inactive models receive current inputs but are not advanced continuously.
Their internal states are updated only when they become active through a mode switch.
The state transfer in \texttt{\_switch\_mode()} is therefore the point where the target model is synchronized with the previously active model.

Output propagation follows the opposite direction.
After the active model has advanced, \texttt{\_update\_output\_states()} copies the active model outputs to the wrapper ports.
The surrounding \texttt{System} therefore reads outputs only from the \texttt{MultiComponent}.
Event ports are handled in the same update step.
Ports corresponding to triggered events are set to \texttt{True}, while inactive event ports are reset to \texttt{False}.

State access is routed through the active model.
\texttt{get\_state()} returns the state of \texttt{active\_comp}.
\texttt{set\_state()} adapts the provided state to the active mode and writes it to \texttt{active\_comp}.
The wrapper therefore provides one access point, while the concrete state schema remains defined by the active model and the subclass adaptation logic.

The hybrid hooks are delegated to the active model as well.
Event indicator evaluation, crossing detection, rollback snapshots, event handling, and internal event hints are all forwarded to \texttt{active\_comp}.
Mode switching can be disabled through \texttt{\_allow\_mode\_switching}.
The hybrid algorithm uses this flag during trial steps so that event detection and localization do not change the active model while a rollback snapshot is valid.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\subsection{Verification}
\label{sec:impl_multi_model_verification}

The verification scenario isolates the switching mechanism through a two-mode integrator with a known analytical reference.
The wrapper holds two scalar integrators that share the input and output interface but apply different gains to the constant input $u = 1$.
Mode~A integrates with $k_A = 1.0$ and Mode~B with $k_B = 2.0$.
Both models share the state variable $x$ with $x(0) = 0$.
A \texttt{mode\_selector} switches from Mode~A to Mode~B at the analytical time $t_s = 0.5\,$s.

The expected trajectory is piecewise linear with slope $k_A$ before the switch and slope $k_B$ after.
The state must remain continuous at $t_s$, since \texttt{\_switch\_mode()} adapts and transfers the active state to the target model before the next time step.

Figure~\ref{fig:impl_multi_model_verification} shows the numerical trajectory against the analytical reference and the active mode over time.

\begin{figure}[t]
    \centering
    \includegraphics[width=\linewidth]{figures/5_implementation/multi_component/multi_component_switching_verification.pdf}
    \caption{Verification of runtime mode switching on a two-mode integrator.
    The numerical trajectory matches the analytical reference.
    The dashed line marks the accepted switch time.
    The state remains continuous across the switch.}
    \label{fig:impl_multi_model_verification}
\end{figure}

The numerical trajectory matches the analytical solution within machine precision, with a maximum absolute error of $3.3\cdot 10^{-16}$.
The active mode flips from A to B exactly at $t_s$.
The recorded \texttt{sync\_events} entry confirms a single accepted switch with retrieved and synchronized states equal to the analytical value $x(t_s) = 0.5$.
The hysteresis path is covered by the unit tests and is not repeated here.